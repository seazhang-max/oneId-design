---
description: OneID 设计规范 - SaaS 电商三层架构
globs: 
alwaysApply: true
---

# OneID 设计规范

## 业务背景

我们是 SaaS 电商平台，采用三层组织架构：
- **全局层 (Global)**：最高层级，跨集团聚合
- **集团层 (Group)**：中间层级，跨品牌聚合
- **品牌层 (Brand)**：最底层级，只有这一层有真实用户

## 核心设计原则

### 1. 层级关系

- **品牌层**：真实用户存在层，数据范围为单个品牌的原始数据
- **集团层**：数据范围为该集团下所有品牌的原始数据，通过共享 ID 标识识别跨品牌的同一自然人
- **全局层**：数据范围为所有集团的原始数据，通过共享 ID 标识识别跨集团的同一自然人

**数据流向**：三层各自独立基于原始底层数据计算，数据范围逐层扩大（品牌 < 集团 < 全局），无层级间的数据依赖

### 2. 独立计算，共享数据

- 三层各自直接从原始 ID 标识数据出发，独立执行 WCC 算法
- 共享同一批底层用户数据（来自 BTC 交易系统）
- 每层有自己的 OneID 空间和映射关系
- 各层的数据范围边界：
  - 品牌层：仅该品牌的原始数据
  - 集团层：该集团下所有品牌的原始数据
  - 全局层：所有集团的原始数据

### 3. 跨品牌/集团合并逻辑

**集团层合并规则**：
- 集团层直接基于该集团下所有品牌的原始 ID 数据执行 WCC 连通
- 同一自然人（通过手机号、邮箱等 ID 标识关联）在同一个集团下的不同品牌有账户时，会被识别为同一个集团 OneID
- 不依赖品牌层的 OneID 结果，直接从原始数据计算

**全局层合并规则**：
- 全局层直接基于所有集团的原始 ID 数据执行 WCC 连通
- 同一自然人在不同集团有账户时，会被识别为同一个全局 OneID
- 不依赖集团层或品牌层的 OneID 结果，直接从原始数据计算

### 4. BTC 交易系统数据参与

BTC 交易系统提供两类数据：

**ID 标识数据**（用于 ID 关联）：
- 手机号
- 邮箱
- 设备 ID
- OpenID / UnionID
- Cookie ID
- 其他第三方 ID

**行为数据**（用于画像丰富）：
- 订单数据
- 支付记录
- 浏览行为
- 购买偏好
- 会员等级


### 6. OneID 生成流程

参考 `流程设计.md` 中的标准流程：

1. **数据采集与标准化清洗**
   - 归一化（大小写、去空格）
   - 脏数据过滤（测试数据、爬虫数据）
   - 构建带权重/时间戳的边表

2. **图数据加载与预处理 (GraphFrames)**
   - 超级节点剪枝
   - 显式缓存顶点和边
   - 设置 checkpoint 目录

3. **WCC 算法执行**
   - 连通分量算法生成候选 OneID
   - 多轮迭代，支持断点恢复

4. **ID 映射决策与冲突消解**
   - JOIN 历史 OneID 库进行增量比对
   - 融合/分裂判定（按权重+时效+优先级）
   - 分配新 OneID / 拆分旧 ID

5. **属性融合与画像沉淀**
   - 多源属性对齐（高优覆盖、多值聚合、时间衰减）
   - 标签体系挂载（会员等级、生命周期、兴趣偏好）
   - 质量校验门禁

6. **服务发布与缓存预热**
   - KV 存储（Redis/HBase）用于实时点查
   - OLAP（ClickHouse）用于分析查询
   - 数仓（Hive/MaxCompute）用于离线挖掘
   - 热点缓存预热、版本快照与回滚

### 7. 实时链路补充

- 新用户事件通过 Flink 实时生成临时 OneID（秒级响应）
- 次日离线 WCC 校准融合，更新正式 OneID

## 技术约束

- 使用 GraphFrames 进行图计算
- 支持增量更新和历史回溯
- 三层 OneID 需要独立的 ID 空间管理
- 支持跨层级的 ID 映射查询

## 数据模型要点

### OneID 结构（三层统一）
```
OneID {
  one_id: String,
  scope: String,        // "brand" | "group" | "global"
  scope_id: String,     // 品牌ID / 集团ID / "global"
  id_mappings: List<IdMapping>,  // 手机号、邮箱等原始ID标识
  profile: UserProfile,
  created_at: Timestamp,
  updated_at: Timestamp
}
```

### 跨层映射表（独立存储）
```
CrossLayerMapping {
  brand_one_id: String,
  group_one_id: String,
  global_one_id: String,
  mapping_type: String,  // "brand_to_group" | "brand_to_global" | "group_to_global"
  confidence: Float,     // 映射置信度
  created_at: Timestamp,
  updated_at: Timestamp
}
```

**说明**：
- 三层 OneID 结构完全独立，不包含对其他层 OneID 的引用
- 跨层关联关系通过独立的映射表维护
- 映射表在 WCC 计算完成后，通过 ID 标识的交集自动生成


## 注意事项

- 只有品牌层有真实用户，集团和全局层是聚合层
- 三层可以完全并行计算，无数据依赖关系
- 各层对"同一个人"的判定可能不同，这是预期行为（因为数据范围不同，连通分量结果不同）
- 跨品牌/集团合并时，需要处理 ID 冲突
- 实时链路和离线链路需要最终一致性保证
- 跨层映射表需要在各层 OneID 计算完成后单独生成和维护
